import packageJson from '../../package.json';

/** Canonical Labs site URL for attribution links. */
export const LABS_URL = 'https://labs.ainativeschool.org';

/** Semver from apps/web/package.json, shown in the site footer. */
export const APP_VERSION = packageJson.version;
